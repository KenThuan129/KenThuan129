# ═══════════════════════════════════════════════════════════════════════════════
# 🐍 CONTRIBUTION SNAKE ANIMATION WORKFLOW
# ═══════════════════════════════════════════════════════════════════════════════
# This workflow generates an animated snake that eats your GitHub contributions
# 
# 📁 File location: .github/workflows/snake.yml
# ═══════════════════════════════════════════════════════════════════════════════

name: Generate Snake Animation

on:
  # Runs automatically every 12 hours
  schedule:
    - cron: "0 */12 * * *"
  
  # Allows manual trigger from Actions tab
  workflow_dispatch:
  
  # Runs on every push to main branch
  push:
    branches:
      - main

jobs:
  generate:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    
    steps:
      # Step 1: Generate the snake SVG
      - name: Generate Snake Graph
        uses: Platane/snk/svg-only@v3
        with:
          github_user_name: ${{ github.repository_owner }}
          outputs: |
            dist/github-contribution-grid-snake.svg
            dist/github-contribution-grid-snake-dark.svg?palette=github-dark
            dist/ocean.gif?color_snake=cyan&color_dots=#00ff88,#00d9ff,#0d1117
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # Step 2: Push the generated files to output branch
      - name: Push to Output Branch
        uses: crazy-max/ghaction-github-pages@v4
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
